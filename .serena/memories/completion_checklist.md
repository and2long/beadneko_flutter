# When a Task Is Completed

- Run `dart format lib test` if you changed Dart files.
- Run `flutter analyze` for linting.
- Run `flutter test` if changes could affect logic or UI tests.
- For UI changes, consider running the app with `flutter run` to validate behavior.
